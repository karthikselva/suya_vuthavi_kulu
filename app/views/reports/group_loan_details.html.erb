Groups : <%= collection_select :select, :group, @groups, :id, :name, {:prompt => "Select"}, {:onchange => "load_group_loan_details(this)"} %>

<div id="result"></div>	

<script>
  function load_group_loan_details(ele)
  {
  	group_id = ele.value
  	if(group_id != "")
  	{
  		$.ajax
    ({
		  url: '<%= load_group_loan_details_reports_path %>',
		  data: { "group_id": group_id },
		  success: function(data) 
      {
		    $('#result').html(data);
		  }
		});
  	}
  	else
  	{
  		$('#result').html("")
  	}  
  }
</script>
